; ============================================================================
; SECRETS.INI.EXAMPLE - Template for OTA credentials
; ============================================================================
; Copy this file to secrets.ini and fill in your OTA password.
; secrets.ini is in .gitignore and will NOT be committed.
; ============================================================================

[env:esp32s3_n16r8]
upload_flags = 
    --port=3232
    --auth=your_ota_password
